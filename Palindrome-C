#include <stdio.h>

int main()
{
    int n=000,r,sum=0;
    
    
    while(n>0)
    {
        r=n%10;
        sum=sum*10+r;
        n=n/10;
    }
    if(n==sum)
    {
        printf("it is palindrome");
    }
    else
    {
        printf("not palindrome");
    }
}
